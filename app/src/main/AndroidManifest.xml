<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.application">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WhatsappMessages"></activity>
        <activity android:name=".FragmentPage" />
        <activity android:name=".insertShifts" />
        <activity android:name=".ListViewPage" />
        <activity android:name=".SendSMS" />
        <activity android:name=".WiningPage" />
        <activity android:name=".SecondLevel" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomePage" />
        <activity android:name=".SignupPage" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>